#include <bits/stdc++.h>
using namespace std;
#include <atcoder/all>
using namespace atcoder;
using ll = long long;

int main()
{
	int n, d;
	cin >> n >> d;

	vector<int> t(n);
	for (int i = 0; i < n; ++i)
		cin >> t[i];
	
	int ans = -1;
}